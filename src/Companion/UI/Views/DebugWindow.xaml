<Window x:Class="PWCompanion.UI.Views.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PW Companion — Debug"
        Height="520" Width="640"
        Background="{StaticResource ButlerBackground}"
        Foreground="{StaticResource ButlerText}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="18" FontWeight="Bold" Foreground="{StaticResource ButlerAccent}"
                   Text="Filipe's Sarcastic PW Butler — Debug Console"/>
        <TextBlock Grid.Row="0" Margin="0,28,0,12" TextWrapping="Wrap" Foreground="{StaticResource ButlerMuted}"
                   Text="Read-only memory inspection. If values look wrong, update offsets.json."/>

        <GroupBox Grid.Row="1" Header="Status" Margin="0,0,0,8" Padding="8">
            <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap"/>
        </GroupBox>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GroupBox Header="Player State" Padding="8" Margin="0,0,4,0">
                <TextBlock Text="{Binding PlayerText}" FontFamily="Consolas"/>
            </GroupBox>
            <GroupBox Grid.Column="1" Header="Offsets" Padding="8" Margin="4,0,0,0">
                <TextBlock Text="{Binding OffsetText}" FontFamily="Consolas"/>
            </GroupBox>
        </Grid>

        <GroupBox Grid.Row="3" Header="Activity Log" Padding="8" Margin="0,0,0,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding LogText}" FontFamily="Consolas" FontSize="11"
                           TextWrapping="Wrap" Foreground="{StaticResource ButlerMuted}"/>
            </ScrollViewer>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Inspect Memory" Padding="12,6" Margin="0,0,8,0" Click="OnInspectMemory"/>
            <Button Content="Reload Offsets" Padding="12,6" Margin="0,0,8,0" Click="OnReloadOffsets"/>
            <Button Content="Close" Padding="12,6" Click="OnClose"/>
        </StackPanel>
    </Grid>
</Window>
